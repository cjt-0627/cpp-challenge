#include<iostream>
#include<algorithm>
using namespace std;
int dice[20];

int main()
{
    int m,n;
    int a,b;
    int f,t,r;//front,top,right
    for(int i=0;i<m;i++)
        dice[i]=1;
    cin>>n>>m;
    for(int i=0;i<m;i++)
    {
        cin>>a>>b;
        if(b>0)
        swap(dice[a],dice[b]);
        else
        {
            if(b==-1)
            {
                if
            }
        }


    }



}